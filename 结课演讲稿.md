# 《人工智能Agent开发入门》结课演讲稿

---

## 各位老师、同学们，大家好！

我是[你的姓名]，[你的班级]的大二学生。今天很荣幸能够站在这里，和大家分享我在《人工智能Agent开发入门》这门课中的学习收获和项目成果。

说实话，在选这门课之前，我和很多同学一样，觉得AI就是问答机器人，问什么答什么。但经过这七次课的学习，我发现AI的世界远比我想象的要精彩得多。

---

## 一、我学到了什么

### 1.1 提示词是打开AI的钥匙

以前我也会用AI，但经常遇到的情况是：同一个问题，别人能得到很棒的答案，我却得到一堆废话。后来我才明白，问题不在AI，而在我"问"的方式。

老师教给我们六大提示工程技法，其中我最喜欢的是**角色定义法**。

举个例子：

❌ 以前我会问："帮我写Python代码"

✅ 现在我会这样问：
> "你是一个经验丰富的Python开发专家，擅长编写清晰、可维护的代码。我是计算机专业的大二学生，正在学习AI智能体开发。请帮我创建一个函数，实现读取txt文件并统计行数，要求处理文件不存在的情况，添加完整的函数注释。"

结果完全不一样！这就是提示词的魔力。

### 1.2 大语言模型不只是一个黑盒子

通过这门课，我终于理解了AI模型背后的参数含义：

- **7B、13B** —— 参数量，越大越聪明，但也更吃配置
- **Q4量化** —— 让大模型在普通电脑上也能跑起来
- **Context Length** —— AI能记住多少内容

我还学会了配置本地LLM。用LMStudio + Qwen3-8B，8GB显存就能跑起来了，非常方便！

### 1.3 Agent不只是聊天机器人

这是我最惊喜的发现！Agent = LLM + 工具 + 记忆 + 规划。

普通AI只能"说话"，但Agent能"行动"！它可以：
- 读写文件
- 访问网页
- 查询数据库
- 调用其他API

这就是Function Call的魅力——让AI拥有了"手"和"脚"。

---

## 二、我的项目成果

说了这么多，可能大家觉得都是理论。让我给大家展示一下我实际做的东西。

### 2.1 项目结构

我的GitHub仓库：[https://github.com/username/ai-agent-course](https://github.com/username/ai-agent-course)

项目包含七个实践模块：

```
ai-agent-course/
├── practice01/     ← 基础LLM调用
├── practice02/     ← 交互式聊天 + 5个工具函数
├── practice03/     ← 上下文压缩 + 知识提取
├── practice04/     ← AnythingLLM知识库集成
├── practice05/     ← 自动知识管理
├── practice06/     ← Skill系统
├── practice07/     ← 完整Agent智能体
└── README.md
```

### 2.2 让我重点说说几个功能

#### 【播放演示视频或现场演示】

**功能1：文件操作工具**

我开发了5个文件操作工具：
1. list_files —— 列出目录文件
2. rename_file —— 重命名文件
3. delete_file —— 删除文件
4. create_file —— 创建文件
5. read_file —— 读取文件

有了这些工具，我就可以用自然语言让AI帮我管理文件了！比如我说"帮我看看practice01目录里有什么"，AI就会调用list_files函数，返回文件列表。

**功能2：上下文压缩**

大家有没有发现，和AI聊久了它会"失忆"？

我的解决方案是：当对话超过5轮，自动压缩前面的内容。前70%生成摘要，后30%保留原文。这样AI就能"记住"更多东西了。

**功能3：知识提取**

我还有个更棒的功能——每5轮对话，AI会自动提取关键信息，按5W规则（谁、做了什么、何时、何地、为什么）保存到本地。

这样我就可以随时问"上次我们讨论了什么"，AI会从本地日志中找到答案。

**功能4：集成知识库**

我还集成了AnythingLLM，可以查询本地文档库。AI不仅能回答通用问题，还能"翻阅"我上传的资料给出准确答案。

### 2.3 代码示例

给大家看一个核心代码片段——Function Call的实现：

```python
tools = [
    {
        "type": "function",
        "function": {
            "name": "list_files",
            "description": "列出指定目录下的所有文件",
            "parameters": {
                "type": "object",
                "properties": {
                    "directory": {
                        "type": "string",
                        "description": "目录路径"
                    }
                },
                "required": ["directory"]
            }
        }
    }
]

# 当AI决定调用工具时
if tool_call.name == "list_files":
    result = list_files(tool_call.arguments["directory"])
```

看起来代码不长，但背后的逻辑很精妙：用户说自然语言 → AI理解意图 → 选择工具 → 执行函数 → 返回结果。

---

## 三、我的学习心得

### 3.1 从"搜索思维"到"对话思维"

以前遇到问题，我第一反应是去搜索引擎找答案。现在我会想：能不能让AI帮我解决这个问题？

但我也认识到，**AI是助手，不是替代者**。核心逻辑、架构设计、质量把控，这些还是需要人来完成。AI只是让这个过程更高效了。

### 3.2 遇到过的坑

说实话，学习过程并不一帆风顺。

**坑1：模型响应不稳定**

有时候同样的提示词，得到的结果质量差很多。

解决方法：优化提示词，增加更多约束条件；用System Prompt固定AI角色。

**坑2：上下文不够用**

长对话后AI会"遗忘"。

解决方法：实现上下文压缩机制，定期保存关键信息。

**坑3：工具调用失败**

Function Call有时候会参数错误或调用失败。

解决方法：完善参数校验，添加详细的错误处理。

### 3.3 这门课教会我的

我觉得这门课最重要的收获，不是某个具体技能，而是**一种新的思维方式**——如何与AI协作，如何用AI增强自己的能力。

这就像学会了使用搜索引擎、学会了编程一样，是21世纪的基础技能。

---

## 四、未来规划

### 4.1 短期（本学期）

1. 完善现有项目，优化代码质量
2. 准备参加"中国大学生计算机设计大赛"
3. 整理学习笔记，写成教程发布到GitHub

### 4.2 中长期

1. 学习深度学习基础，了解Transformer原理
2. 尝试微调开源模型
3. 开发更多实用工具，积累项目经验

---

## 五、结语

最后，我想用一句话总结我的感受：

> **AI不会取代你，但会用AI的人会取代你。**

通过这门课，我学会了如何与AI对话，如何让AI帮我做事。这让我对未来充满了信心。

感谢老师的悉心指导，感谢同学们的帮助！

我的汇报就到这里，请各位老师批评指正。谢谢大家！

---

## 【演讲提示】

### 时间控制
- 开场自我介绍：2分钟
- 学到了什么：5分钟
- 项目成果展示：5分钟
- 学习心得：3分钟
- 未来规划：1分钟
- 结语：1分钟
- **总计：17分钟**

### 演讲技巧
1. **放轻松** —— 这是分享自己的学习经历，不是考试
2. **多互动** —— 适当问观众"大家有没有遇到过这种情况"
3. **展示代码** —— 关键代码可以放截图或投影
4. **演示demo** —— 如果条件允许，现场演示Agent效果会很加分
5. **真实真诚** —— 分享自己踩过的坑，比只讲成功经验更打动人

### 准备清单
- [ ] 演讲稿（打印纸质版）
- [ ] PPT或演示文稿
- [ ] GitHub仓库准备好
- [ ] 代码演示环境测试过
- [ ] 提前熟悉场地和设备

---

**演讲人**：[你的姓名]  
**班级**：[你的班级]  
**日期**：2026年5月18日
